export type ColorsType = {
  primary: string;
  background: string;
  card: string;
  text: string;
  border: string;
  notification: string;
  secondary: string;
  success: string;
  warning: string;
  error: string;
  info: string;
  grey: string;
  lightGrey: string;
  darkGrey: string;
  transparent: string;
};

/**
 * proper type not working because of using colors =
 * use theme which is not typed
 */
